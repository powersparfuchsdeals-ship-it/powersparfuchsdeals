name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 8 * * 1,4"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping live site
        run: curl -fsS https://deals.orbital-noir.com/api/health || true
